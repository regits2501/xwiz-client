language: node_js
node_js:
 - '8.6.0'
git:
  depth: 3
before_install:
 - sudo apt-get install -y xvfb
 - npm i -g npm@5.6.0
  
addons:
  apt:
    packages:
      - firefox
cache:
  directiories:
    - 'node_modules'

